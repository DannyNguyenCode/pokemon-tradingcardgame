services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./app:/app/app
    environment:
      - PYTHONUNBUFFERED=1
    command: gunicorn --bind 0.0.0.0:8000 main:app --workers 1 --reload --timeout 300 --log-level debug --capture-output --access-logfile -
  # game-server:
  #   build:
  #     context: ../api-game
  #     dockerfile: Dockerfile
  #   ports:
  #     - "3001:3001"
  #   environment:
  #     - PORT=3001
  #     - ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000,https://pokemon-tradingcardgame.vercel.app
